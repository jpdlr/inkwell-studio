# Agent Instructions

## Project goals
- Maintain a calm, editorial layout with clear hierarchy.
- Preserve the teal + tangerine color system.
- Keep the page fast and lightweight (CSS-first, minimal JS).

## Workflow
- Update copy in `src/App.jsx`.
- Adjust styles in `src/styles.css`.
- Add tests alongside `src/App.test.jsx` as needed.

## Standards
- Prefer semantic HTML.
- Keep animations subtle and purposeful.
- Avoid adding large dependencies unless requested.
